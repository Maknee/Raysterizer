#include "include/descriptor_pool.h"

namespace RaysterizerEngine
{
	/*
	Expected<vk::DescriptorPool> DescriptorPool::Allocate(vk::Device device, const DescriptorSetLayoutCreateInfo& descriptor_set_layout_create_info)
	{

	}

	Error DescriptorPool::Release(vk::Device device, vk::DescriptorPool pool)
	{
		device.destroyDescriptorPool(pool);
		return NoError();
	}
	*/
}